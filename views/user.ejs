<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>

<body>
    <h1>Data</h1>
    <%= user.firstName %>
    <%= user.lastName %>
    <%= user.email %>
    <% if (user.mobileno.length === 10) { %> <%= user.mobileno %> <% } 
        else { %> invalid mobilenumber... <% } %>              
</body>

</html>